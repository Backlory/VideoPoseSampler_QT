# VideoPoseSampler v1.0.0-alpha

A server that communicates simultaneously with NDI Aurora and OpenCV VideoCapture Device.


Params：

   -s saveFolderPath, where to save all the data
   
   --roi x,y,w,h, the ROI of the image
